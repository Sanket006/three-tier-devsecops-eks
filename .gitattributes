# Set default behavior to handle line endings automatically.
* text=auto

# Declare files that will always have LF line endings on checkout (prevents script execution issues on Linux).
*.sh text eol=lf
Jenkinsfile* text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
